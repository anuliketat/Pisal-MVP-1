{
  "name": "fin-alert-parse-stub",
  "version": "0.1.0",
  "private": true,
  "description": "Optional backend: POST /parse/batch (stub rules; add HF here — never ship tokens in the app).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
